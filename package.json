{
  "name": "apitest",
  "version": "1.0.0",
  "description": "To build: $ gulp To serve: $ python -m SimpleHTTPServer      then navigate to localhost:8000",
  "main": "apitest.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-browserify": "^0.5.1",
    "gulp-concat": "^2.6.0",
    "gulp-jshint": "^2.0.0",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "^2.1.1",
    "gulp-shell": "^0.5.1",
    "gulp-uglify": "^1.5.1",
    "jshint": "^2.8.0"
  }
}
